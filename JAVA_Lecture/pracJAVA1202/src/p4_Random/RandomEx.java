package p4_Random;

import java.util.Random;

public class RandomEx {
	//Random은 Serializable 을 임플리먼트한 클래스다.
	//동기화 -> 네트워크에서 사용함
	//seed 즉, 종잣값이 있음. 이것이 있는게 Math.random()과 차이가 있음.
	public static void main(String[] args) {
		Random r1 = new Random();
		
		
	}
}
